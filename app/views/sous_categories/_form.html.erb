<%= form_with(model: sous_category) do |form| %>
  <% if sous_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sous_category.errors.count, "error") %> prohibited this sous_category from being saved:</h2>

      <ul>
        <% sous_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :titre, style: "display: block" %>
    <%= form.text_field :titre %>
  </div>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
